---
name: handoff-2026-06-26d
description: Session handoff — Milestone 2 re-scoped to commands/ directory; 4 wrong-path PRs closed; round-sizing heuristic added
metadata:
  type: project
---

## Completed this session

- Resumed orchestrator for Milestone 2 (Skill Files) and launched Round A: T-013, T-014, T-015, T-025 in parallel worktrees
- All 4 Round A agents completed and opened PRs (#2–#5) — but wrote to wrong paths (`agentflow/prompts/`, `agentflow/skills/providers/claude/`)
- Discovered `commands/` is the canonical git-tracked skill directory (not `agentflow/skills/providers/`)
- Resolved: skill files live in `commands/`, no pip package distribution (IP protection deferred)
- Resolved: Gemini provider deferred; Claude-only for v1 skill validation
- Resolved: orchestrator round-sizing heuristic — `max_tasks = max(1, (threshold - current_tokens) / tokens_per_task_estimate)`
- Closed PRs #2–#5 (superseded), removed all 4 worktrees + branches
- Updated `tasks.json`: T-013/014/015/025/026/027 owns paths corrected to `commands/`
- Updated `architecture.md`: skill location RESOLVED, Gemini DEFERRED, round-sizing RESOLVED, `tokens_per_task_estimate: 2500` in config schema
- Re-spar via /oracle confirmed design changes

## Key decisions

- **Skill files → `commands/`** not Python package: `commands/` is git-tracked, users copy to `~/.claude/commands/`. No importlib.resources loading for skills — that was conflating Python module prompts (loaded by checklist.py etc.) with slash command skill files.
- **`agentflow/prompts/oracle/v1/` is still valid** for Python-loaded prompts (used by `conversation.py`, `artifact_generator.py` via importlib.resources) — separate concern from the `commands/` skills.
- **Round-sizing heuristic** added to T-027/orchestrate.md spec: before each round, cap tasks based on remaining token budget. Prevents mid-round handoff disruption.
- **tokens_per_task_estimate: 2500** added to config schema (T-002).
- T-013 owns `commands/oracle/checklist.md`, `market.md`, `generation.md` — sub-files read by `commands/oracle.md` at runtime. Persona/system inlined in T-026's oracle.md.

## Open items / next steps

- Re-run `/orchestrate` for Milestone 2 Round A with corrected `commands/` paths
- Round A: T-013, T-014, T-015, T-025 (all unblocked — parallel)
- Round B: T-026, T-027 (after Round A — update commands/oracle.md and commands/orchestrate.md)
- After Milestone 2: test `/oracle` and `/orchestrate` manually to validate token savings hypothesis
- Milestone 3 (Config + PTY Shell) and beyond remain pending

## Codebase state

`main` is clean at `0e6f7ba`. Milestone 1 (T-001 package scaffold) is complete. Milestone 2 tasks T-013–T-027 are all pending with corrected `commands/` paths in tasks.json. The `commands/` directory has 4 working skill files (oracle.md 232 lines, orchestrate.md 422 lines, handoff.md 92 lines, drift.md 92 lines) that are the baseline to update. The `agentflow/prompts/` directory contains Python-loaded prompt stubs (used by oracle/conversation.py etc.) — these are a separate concern and still valid. No active worktrees.
