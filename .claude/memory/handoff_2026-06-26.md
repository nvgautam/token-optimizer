---
name: handoff-2026-06-26
description: Session handoff — expanded token savings to 9 strategies, moved indexer + context builder to v1, added IP protection gap as UNRESOLVED
metadata:
  type: project
---

## Completed this session
- Added `## Persona` section to `commands/orchestrate.md` (Staff Engineering Lead — executes plan faithfully, does not re-prioritize)
- Expanded token savings model from 5 to 9 strategies in `architecture.md`
- Moved context bundle and symbol index from v2 → v1 (maximum savings goal)
- Added 4 new strategies: compact state docs, verbosity control, section-only loading, per-session-type thresholds
- Added 3 new tasks: T-028 (parsers), T-029 (index manager + brownfield scanner), T-030 (context builder)
- Updated T-002, T-008, T-013, T-014, T-015, T-025 descriptions + test requirements for new strategies
- Added per-session threshold fields to config schema (`oracle_threshold_tokens: 60000`, `orchestrator_threshold_tokens: 30000`)
- Added `Skill IP protection` and `Token savings validation` as UNRESOLVED items in architecture.md
- Trimmed CLAUDE.md: removed v2 block and v2 integrations (both fully captured in architecture.md)
- Collapsed redundancy in architecture.md design decisions log

## Key decisions
- **IP protection gap identified**: `.md` skill files are plain text — not IP-protected. PTY binary only protects shell mechanics. Mechanism for protecting skill content (embed+inject at runtime vs server-side delivery) is UNRESOLVED before commercial distribution.
- **Immediate priority**: Manual testing of oracle + orchestrate + handoff skills to validate token savings hypothesis BEFORE further implementation. Pass criteria: measurable reduction in accumulated tokens vs no-handoff baseline.
- **v1 scope expanded**: Context builder and symbol indexer moved to v1 for maximum token savings. Combined 9-strategy model estimated to yield ~2× more work per session vs baseline.
- **Per-session thresholds**: Oracle (conversational) = 60K, Orchestrator (file-heavy) = 30K, unknown session type = 40K global fallback.
- **CLAUDE.md scope rule**: Should be a session guide only — no project tracking, no v1/v2 roadmap. Those belong in architecture.md.

## Open items / next steps
- **Commit pending changes**: CLAUDE.md, architecture.md, tasks.json, commands/orchestrate.md, agentflow_ledger.json are modified but uncommitted
- **Manual skill testing**: Test oracle → handoff → resume flow; measure token savings vs no-handoff baseline. Validation gate before further build.
- **IP protection**: Design mechanism for protecting skill content before commercial distribution (UNRESOLVED)
- **state.json stale**: References groups with task IDs (T-003–T-005, T-009–T-024) that no longer exist in tasks.json — needs reset before next /orchestrate run
- **Implementation**: 13 tasks pending — T-002, T-006, T-007, T-008, T-013, T-014, T-015, T-025, T-026, T-027, T-028, T-029, T-030

## Codebase state
T-001 (package scaffold) is the only completed task. Five files are modified and uncommitted: CLAUDE.md (trimmed v2 block + integrations), architecture.md (9-strategy token savings model, IP protection UNRESOLVED, config schema with per-session thresholds), tasks.json (3 new tasks + updates to 6 existing tasks, 4-round parallelism plan), commands/orchestrate.md (persona section added), agentflow_ledger.json. No worktrees. No escalations. The `.agentflow/state.json` is stale — references task groups from a prior session that included tasks now removed from tasks.json; must be reset before next orchestration run.
