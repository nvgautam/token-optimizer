---
name: handoff-2026-07-01
description: Fixed savings-report double-counting/fabrication bugs, then found the verbosity-savings figure is still an unscoped lifetime sum vs. an unmeasured 600-token baseline — T-081 added to A/B-test it
session_type: unknown
---

## Completed this session
- Earlier session: diagnosed why `agentflow report --mode aggregate` drifted 65% → 72% with no real efficiency change
  - Root cause 1: `read_logger.py` (PreToolUse) logs every Read attempt before `read_check.py` (PreToolUse) can block it; blocked full-read attempts (never executed) were counted as both phantom "real" full-read cost AND phantom "targeted-read" savings
  - Root cause 2: `compression_real` fell back to a hardcoded `15000` whenever Headroom's `total_tokens_after` was 0 but `total_tokens_saved` was nonzero — fabricated denominator
  - Fixed `agentflow/reporting/report_builder.py`: filter blocked-attempt rows at load time; replaced waste-bucket contribution to `total_saved` with genuine `baseline − real`; excluded compression from totals when unmeasured instead of fabricating it
  - Updated `tests/reporting/test_report_builder.py`; full suite 447 passed (2 pre-existing unrelated failures)
  - Regenerated `combined_report.html`: corrected aggregate 74.3% → currently shows 74.4% (drift is from new log entries accruing during this later session, not a new bug)
- This session (oracle sparring, triggered by user flagging `combined_report.html` as suspicious): traced `verbosity_savings` (2,686,683) and `idx` (0) line-by-line against source and raw logs
  - `idx: 0` is **not a bug** — `get_bucketed_stats()`'s `targeted-reads` bucket is a violation/waste counter (idx existed, offset not given); `read_check.py` now hard-blocks 100% of those before they execute, so it's structurally pinned near zero = full compliance, mislabeled as if it were a savings line. Real targeted-read savings (~724K–736K tokens) are correctly captured in `file_reads_saved`, just not surfaced under the "idx" label.
  - `verbosity_savings: 2,686,683` **is a real bug**: `report_builder.py:54` sums `max(0, 600 − output_tokens)` over the *entire lifetime* `verbosity_log.jsonl` (5,351 turns since 2026-06-29), where 600 is architecture.md's modeled, never-measured no-hook baseline. That unvalidated, unscoped figure gets summed directly into `total_saved` next to `file_reads_saved` (scoped to current `shadow_reads.jsonl`), and accounts for ~79% of the reported headline — apples-to-oranges.
  - Added **T-081** to `tasks.json` / `execution_plan.md` (pending, depends on T-072/T-073): gate `verbosity_reminder.py` behind an env var, run a controlled hook-on/hook-off comparison, replace the hardcoded 600 constant with the measured baseline, add confidence-interval reporting, and fix the lifetime-vs-windowed scope mismatch.

## Key decisions
- Excluded unmeasurable compression savings entirely rather than fabricate a denominator — don't invent numbers when the real value is unknown
- Filtered blocked-attempt entries at the single choke point in `report_builder.py` rather than touching `read_logger.py`'s hook timing — smaller blast radius, same effect
- Left `analyzer.py`'s `get_bucketed_stats` waste-bucket semantics untouched — legitimate opportunity/waste metrics; only their incorrect inclusion in `total_saved` was previously the bug, and that's already fixed
- Did not fix the verbosity scope-mismatch inline this session — scoped it as T-081 instead, since fixing it properly requires empirical A/B data, not another guessed constant

## Open items / next steps
- T-081 (new, pending): A/B test verbosity control, replace the 600-token assumption with measured data
- Residual, still unfixed: split-mode labeling in `report_builder.py`/`analyzer.py` still calls violation counts "compliance"/"idx" — misleading, not fixed (out of scope again this session)
- 4 pending tasks, unrelated: T-063, T-064, T-068, T-069
- No design/orchestrator state changed this session (session_type=unknown — investigation + task authoring only, no code changes to report_builder.py this time)

## State document updated
none (tasks.json and execution_plan.md updated directly for T-081, not via oracle/orchestrator flush)
