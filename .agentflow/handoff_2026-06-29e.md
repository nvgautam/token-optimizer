---
name: handoff-2026-06-29e
description: PTY session manager bug fixes — verbosity banner timing + IDX regex
session_type: unknown
---

## Completed this session
- Fixed static verbosity banner injected before Claude Code TUI is ready (race condition on restart)
- Fixed IDX regex missing `file_path=` keyword-arg format — Claude Code's actual tool display
- Added 4 new tests for banner startup threshold; added 2 new tests for regex coverage
- Removed 2 stale tests that asserted old "first byte" banner behavior

## Key decisions
- Static banner now waits for 1024 bytes of startup output before injecting (gives TUI time to initialize)
- IDX regex now has 3 alternatives: `file_path="..."` (keyword, primary), `Read("...")` (positional), `Read tool path` (NL)
- `_detect_read_path` updated to `next((g for g in m.groups() if g), None)` — handles N groups

## Open items / next steps
- T-051 still pending (in state.json); check tasks.json for definition
- **BUG (unresolved):** All `write_input` injections (IDX banners, verbosity banners) appear in Claude Code's input field and require manual submission — `\n` does not auto-submit in Claude Code's raw-mode TUI. Root cause: Claude Code's input handler expects `\r` (Enter in raw mode), not `\n`. Fix: change all banner line terminators from `\n` to `\r` in `session_manager.py` (`_VERBOSITY_STATIC_BANNER`, `_IDX_BANNER`, and the dynamic banner in `_inject_verbosity_banner`). The 1024-byte startup threshold is still useful (avoids injecting before TUI renders) but is not sufficient on its own.
- Consider whether 1024-byte threshold needs tuning per Claude Code version

## State document updated
none
