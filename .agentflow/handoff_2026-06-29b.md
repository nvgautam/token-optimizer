---
name: handoff-2026-06-29b
description: Bug fix — verbosity banner firing on idle TUI redraws
session_type: orchestrator
---

## Completed this session
- Diagnosed and fixed PTY verbosity banner showing up in user's CLI input field
- Fixed `\n` → `\r` on all `write_input` calls (Claude Code TUI requires CR for submission)
- Removed `tick()` method — it called `_handle_output` on every PTY chunk including idle ANSI escapes, causing banner to fire with no user activity
- Added 30s cooldown to `_inject_verbosity_banner` to prevent cascade on multi-paragraph responses
- Committed: `a39f295 fix: stop verbosity banner firing on idle TUI redraws`

## Key decisions
- Removed proactive 60s timer entirely rather than fixing it — turn-boundary injection + session-start injection is sufficient; timer added noise without value
- Used `\r` (carriage return) not `\r\n` — PTY readline in Claude Code's TUI submits on CR alone

## Open items / next steps
- T-051: CLI integration tests (argparse round-trip, main() dispatch, __main__ smoke) — PENDING
- T-052 still marked PENDING in tasks.json (prior session bookkeeping not updated); state.json shows MERGED
- Verify `\r` actually auto-submits in Claude Code's TUI — not yet tested live

## State document updated
none
