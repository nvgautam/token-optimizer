---
name: handoff-2026-07-18e
description: Oracle session — drain root-cause forensics, T-283/T-284 filed, C-splits2 MERGED, C-pty round created as top pending
session_type: oracle
---

## Completed this session

- **Root-cause analysis: premature drain in session 44f7420a** — confirmed via pty_audit.jsonl that `orchestrate.md` line 39 (`pty_signal.py task_done <task_id>` after worker completes) tombstones TIF too early, before human review gate; drain fires at 102K fill; hook plays no role
- **Two vestiges identified in orchestrate.md:**
  1. `pty_signal task_done` on line 39 — premature, causes early drain; hook owns this at PR merge
  2. `AGENTFLOW_ROUND_COMPLETE` prints (lines 40 and 69) — PTY has no handler; dead signal
- **T-283 filed** — add `sid` to `pty_signal.py` `_log` calls (task_done, task_start, handoff_complete)
- **T-284 filed** — remove vestigial `task_done`/`AGENTFLOW_ROUND_COMPLETE` from orchestrate.md
- **C-splits2 MERGED** (PR #185 T-272, PR #186 T-282) — main fast-forwarded, worktrees pruned
- **Round C-pty [PENDING]** inserted as top pending round: T-283 ‖ T-284 (parallel)
- tasks.json updated: T-282 complete, T-283/T-284 pending

## Key decisions

- `AGENTFLOW_TASK_COMPLETE` and `AGENTFLOW_TASK_START` stdout signals are NOT vestigial — `output_handler.py` uses them for EWMA token cost accounting; only the `pty_signal task_done` Bash call and `AGENTFLOW_ROUND_COMPLETE` prints are removed by T-284
- Previous rounds (T-270/T-271) avoided premature drain because `current_round.json` disappeared at 20:00:15 before TIF went tombstone — the hook was not the cause; fill level alone does not explain it; the vestige fix (T-284) is the correct permanent fix regardless

## Open items / next steps

- **Next round: C-pty** — T-283 ‖ T-284 (parallel); run `/orchestrate`
- **T-260 vs T-279 design question** still deferred — raise before Round C (CLI spike)
- **Design gap partially closed**: after T-284 merges, drain will only fire when hook detects merged PR — but verify that the hook reliably fires for all PR-merge paths (gh pr merge via Bash is covered; manual merge on GitHub relies on next Agent call)

## State document updated

execution_plan.md — C-splits2 marked MERGED, Round C-pty [PENDING] inserted as first pending row, T-283/T-284 addendums appended
tasks.json — T-282 complete, T-283/T-284 pending
