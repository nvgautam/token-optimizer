---
name: handoff-2026-07-02c
description: Oracle re-spar — idx/verbosity savings math clarified, handoff-savings measurement approach resolved, T-085 filed
session_type: oracle
---

## Completed this session
- Clarified two savings-math questions: idx's "65% per targeted read" (per-operation ratio) vs verbosity's "~6% of total pool" (share of combined 10.7M-token savings pool, dwarfed by Headroom's 9M) are different denominators, not comparable directly.
- Flagged that the 6% verbosity figure is still the `[UNMEASURED baseline=600tok, n=0]` T-081 placeholder — not real A/B data.
- Corrected user's assumption that `agentflow report --mode session` already isolates skill-load overhead from conversational marginal cost — it doesn't; it's a session-level real-vs-shadow-compaction aggregate (`legacy_report.py` / `shadow_tracker.py`), no turn-level decomposition.
- Confirmed `verbosity_log.jsonl` has per-turn granularity but only *output* tokens — skill/phase-file-load cost (input tokens via Read) isn't logged per-turn anywhere today.
- Resolved session-recycling (handoff) savings measurement approach and filed T-085.

## Key decisions
- **Session-recycling savings**: don't build new per-turn input-token logging + skill-load tagging (new schema, too much scope for the value). Instead recalibrate architecture.md's triangular-sum handoff formula using the 99 real closed sessions already in `agentflow_ledger.json` — median/p25 `(final_ctx-initial_ctx)/n_turns`, bucketed by `session_type`, replacing the invented 500 tok/turn constant.
- **Conservative bias**: use p25 (not median) per explicit user direction — savings claims should undershoot, not overshoot, because paying customers ($5-20K/month API spend) will reconcile against their own bill. An unlabeled polished percentage doesn't survive that; a labeled methodology ("modeled from N=99 measured sessions, conservative percentile") does.
- Report line item must always carry that methodology label — never presented as directly-measured, consistent with T-083's aggregate-vs-waste labeling split.

## Open items / next steps
- T-085 (pending): build `agentflow/reporting/handoff_savings.py`, wire into `report_builder.py` (currently at the 250-line cap — extend via new module, not inline), update architecture.md's Handoff section with the calibrated constant.
- T-084 still pending (headroom cache-mode compression trade-off — unrelated, filed prior session).
- Verbosity A/B comparison (T-081 mechanism built, not yet run) still outstanding — `n=0` until someone runs it.
- Session-recycling report bucket itself (design_status.md "missing from report" row) is now superseded by T-085's scoped approach — no separate task needed.

## State document updated
`design_status.md` (new RESOLVED row: session-recycling measurement approach), `execution_plan.md` (T-085 added to both tables), `tasks.json` (T-085 task definition appended)
