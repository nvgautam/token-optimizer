---
name: handoff-2026-07-01m
description: Support session — resolved headroom-ai install on Intel macOS (ORT_LIB_LOCATION + ORT_PREFER_DYNAMIC_LINK)
session_type: unknown
---

## Completed this session
- Answered `agentflow report --mode session --agent agy` usage question (Gemini-only session report)
- Confirmed no tracked task exists for "install headroom-ai in .venv" (T-071/T-074 assume it as a step, don't own it) — offered to add one, user chose manual install instead
- Confirmed T-074 already auto-wires `headroom wrap claude` in `cmd_shell` (agentflow/cli.py:103-120) via `shutil.which("headroom")` — no manual invocation needed once installed
- Diagnosed two rounds of `headroom-ai[all]` install failures on Intel macOS; user's install now succeeds

## Key decisions
- No design decisions changed this session — pure troubleshooting/support, design_status.md and execution_plan.md untouched
- Root cause of install failure: zsh glob on unquoted `headroom-ai[all]`, then missing `ORT_PREFER_DYNAMIC_LINK=1` env var (only 2 of 3 required exports were relayed initially) — full sequence confirmed against `.agentflow/headroom_eval.md`:
  ```
  brew install onnxruntime
  export ORT_LIB_LOCATION=$(brew --prefix onnxruntime)/lib
  export ORT_PREFER_DYNAMIC_LINK=1
  pip install 'headroom-ai[all]'
  ```

## Open items / next steps
- headroom-ai now installed in `.venv` on user's machine — T-072/T-074 compression-gain measurement is now actually runnable (previously blocked on install)
- `.headroom/` workspace dir now present (untracked, created by first `headroom wrap` invocation or install) — not yet gitignored, worth checking if it should be
- Stale untracked handoff file `.agentflow/handoff_2026-07-01l.md` still present from a prior session — not cleaned up here (outside this skill's scope)

## State document updated
none
