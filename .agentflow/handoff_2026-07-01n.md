---
name: handoff-2026-07-01n
description: Oracle re-spar — Headroom stale-Read risk to gate files, resolved with T-079
session_type: oracle
---

## Completed this session
- Diagnosed a live /orchestrate incident (garbled-looking Read/Grep on `design_status.md` + `rate_calibration_claude.json`) by reading Headroom's installed source directly, not guessing
- Confirmed `DEFAULT_EXCLUDE_TOOLS` (Read/Glob/Grep/Write/Edit) already protects those tools from ContentRouter compression; `cli.py`'s `headroom wrap claude` invocation passes no flags that would defeat it
- Traced actual risk to `ReadLifecycleConfig` (`compress_stale=True`, file-level not range-level) — rewrites a prior-turn Read of a file into a re-read marker once that file is later edited, which fires often on AgentFlow's own frequently-rewritten living docs
- Confirmed via `_read_covers`/`_classify_reads` that SUPERSEDED is range-aware (off by default anyway) but STALE is file-level only — indexed partial reads still get marked stale by unrelated edits to the same file, a false-positive-safe bias, not a correctness gap
- Confirmed the live "no-reread" savings bucket (`shadow/analyzer.py`, `combined_report.html`) tracks worker task-dependency reads via bundle delivery, not oracle/orchestrator's own Read calls — no conflict with T-079
- Recorded the resolved design decision in `design_status.md`; added **T-079** (harden gate-file reads: skill rule + invocation-drift audit test) to `tasks.json` and `execution_plan.md` M4 addendum, depends on T-074
- Caught that M4's header still said `Status: COMPLETE` while T-079 sat inside it PENDING — orchestrator's "identify first incomplete milestone" resume logic could have silently skipped it; fixed the header to `COMPLETE — addendum: T-079 PENDING`

## Key decisions
- Chose "behavioral rule + invocation audit" over disabling Headroom for skill sessions or forking Headroom's exclusion list — cheapest fix, no new maintenance dependency on Headroom internals, preserves existing T-072/T-074 measurement scope
- Did not treat the user's incident description as literal corrupted/injected tool output requiring a security response — grounded the diagnosis in Headroom's actual source instead of the paraphrased marker text, which didn't match the real STALE/SUPERSEDED marker format found in code

## Open items / next steps
- T-079 (PENDING, depends on T-074/MERGED, owns `commands/claude/oracle.md`, `commands/claude/orchestrate.md`, `tests/test_headroom_invocation.py`) — no ownership conflict blocking it, but shares `orchestrate.md` with pending M5/M6 tasks (T-063/T-064/T-069); addendum "Round I" placement should keep it out of the same round
- Run `/orchestrate` next — it should now pick up T-079 given the corrected M4 status header
- Not yet verified: whether `oracle.md`/`orchestrate.md`'s current line counts (165/209) are meant to fall under the CLAUDE.md "prompts ≤ 150 lines" cap or are exempt as top-level dispatcher files — pre-existing, not caused by this session, but T-079's added rule will push them further over if the cap does apply

## State document updated
design_status.md, tasks.json, execution_plan.md
