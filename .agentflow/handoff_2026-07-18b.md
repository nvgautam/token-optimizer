---
name: handoff-2026-07-18b
description: Oracle session — C-2 premature drain root cause + signal-file audit log gap analysis; T-276 merged, T-277 filed
session_type: oracle
---

## Completed this session

- **C-2 premature drain root cause confirmed**: `_write_merged_and_clear` in `drain_restart.py` returns early at line 18 (`except Exception: return`) when `current_round.json` is unreadable. For the T-266 drain, `current_round.json` was absent/unreadable → early return → TIF not unlinked → stale tombstone `[]` persisted. When Round C-2 started (new `current_round.json` written) and fill hit 80k at 04:38:41, the stale tombstone + round existence triggered the premature drain. Confirmed via 0.5ms gap between `drain_restart_triggered` and `session_restart` (vs 13ms for C-2 which ran fully). No data corruption — T-162/T-268 still pending, execution_plan.md pattern mismatch prevented bad annotation.
- **Signal-file audit log coverage gaps identified**: 8 unlogged operations across `current_round.json`, `task_complete.json`, `handoff_complete.json`, `context_fill.json`. Root systemic miss: `clear_signal_files` logs errors only, never success; T-225/T-226 audited `except:pass` but missed `except Exception: return`.
- **T-276 filed and merged (Round C-P3)**: All 8 gaps documented with exact file/line/fix. PR merged.
- **T-277 filed (Round C-P4)**: Defensive fix for `_write_merged_and_clear` — split `except Exception: return` into `except FileNotFoundError` (fall through, unlink TIF) vs `except Exception` (log + return, preserve TIF for retry). `current_round.json` has no write lock; `JSONDecodeError` from mid-write race is a real failure mode. Retry path is the correct recovery. Three acceptance criteria (two unit tests + one integration test) specified.

## Key decisions

- **FileNotFoundError vs JSONDecodeError split**: `FileNotFoundError` = file genuinely absent → fall through to TIF unlink. `json.JSONDecodeError` / other = corrupt or mid-write race → log + return, preserve TIF for next 30s poll retry. Falling through on `JSONDecodeError` would permanently destroy both files with no annotation recovery.
- **`current_round.json` has no write lock**: Confirmed by grep — only `execution_plan.md.lock` and `tasks.json.lock` exist. Mid-write race is the likely failure mode for corrupt reads.
- **T-277 missed during T-276 implementation**: The FileNotFoundError/JSONDecodeError split was documented in the T-276 spec but was not in the original task row, causing the orchestrator to miss it. Filed as separate C-P4 task.
- **Two audit miss modes identified**: (1) Pattern mismatch — T-225/T-226 audited `except:pass`, missed `except Exception: return`. (2) Scope gap — audit focused on PTY-written signal files, `current_round.json` (skill-written, PTY-detected) was treated as a state transition not a signal file.

## Open items / next steps

- **T-277 (C-P4, solo)**: Fix `_write_merged_and_clear` exception handling — next to run before C-1 or C-2
- **Round C-2 (T-162 ‖ T-268)**: Worktrees exist at `.claude/worktrees/T-162-oracle-split` and `.claude/worktrees/T-268-oracle-dup-check`, ready to continue after T-277
- **Round C-1 (T-266)**: debug.md rewrite — after C-P4 and C-2
- **gitignore cleanup**: `headroom_ab_log.jsonl` + `payload_inspect.jsonl` need `git rm --cached` + `.gitignore` entries — still unfiled

## State document updated

`execution_plan.md` — T-277 added as Round C-P4; T-276 row already MERGED
`tasks.json` — T-276 complete, T-277 pending added
