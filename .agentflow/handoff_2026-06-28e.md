---
name: handoff-2026-06-28e
description: Oracle session — PTY terminal size fix, verbosity bug diagnosis, T-052 scoped
session_type: oracle
---

## Completed this session
- Fixed PTY initial terminal size bug: `pty_wrapper.py` — added `_sync_winsize()` called at fork; `_handle_sigwinch` now delegates to it (was duplicated inline)
- Diagnosed verbosity_log.jsonl empty: turn boundary detection (`\n\n`) never fires against Claude Code ANSI TUI output
- Scoped T-052: Read-event idx injection (ANSI strip + regex) + proactive verbosity banners (session-start + 60s monotonic timer)
- Updated design_status.md: `Read hook enforcement` → RESOLVED (PTY stream detection, T-052)
- Updated execution_plan.md: M4 → IN_PROGRESS; T-052 added as Round F
- Updated tasks.json: T-052 PENDING with full spec

## Key decisions
- Proactive verbosity via `time.monotonic()` timer, not turn counter — avoids broken ANSI turn heuristic
- No ANSI stripping needed for verbosity banners; ANSI stripping scoped only to Read-event detection
- Hook enforcement deferred v2 entry updated: PTY approach makes Claude Code PreToolUse hook moot
- `tick()` method on SessionManager holds timer state; relay loop in cli.py calls it each iteration

## Open items / next steps
- T-052 PENDING — orchestrate to implement; owns `session_manager.py` only
- T-051 PENDING — CLI integration tests (pre-existing)
- state.json reflects M4 COMPLETE — orchestrate should update after T-052 merges

## State document updated
design_status.md, execution_plan.md, tasks.json
