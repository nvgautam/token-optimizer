---
name: handoff-2026-07-02
description: Oracle re-spar diagnosed combined_report.html anomalies (idx=0, compression=0); T-082/T-083 filed, both PENDING; no code changed
session_type: oracle
---

## Completed this session

- User flagged four anomalies in `combined_report.html`: headroom compression stuck at 0, idx-savings bucket stuck at 0, verbosity showing `[UNMEASURED baseline=600tok, n=0]`, and no session-recycling/handoff bucket in the strategy breakdown.
- Root-caused all four via oracle re-spar (`design_status.md` — "Oracle Direction — Sparred 2026-07-02"):
  - **Compression=0**: `report_builder.py` reads `~/.headroom/headroom.db` (SQLite, empty) instead of the populated `.headroom/proxy_savings.json` (1,027 requests, 9,035,421 lifetime tokens_saved) — wrong data source, not a modeling bug. → **T-082**.
  - **idx=0**: correct value, mislabeled. The idx bucket only counts full-reads-despite-available-index (waste), and `read_check.py` + T-081's `_is_blocked_attempt()` now strip those before they reach the analyzer — 0 is the right number once compliance is enforced, but the section header calls it "savings" when it's "waste avoided." → **T-083**.
  - **Verbosity n=0**: not a bug — T-081 built the A/B mechanism but the comparison run hasn't executed yet; `[UNMEASURED]` is the correct fallback state. No task filed.
  - **Missing session-recycling bucket**: real scope gap (handoff/context-cycling was never modeled in `get_bucketed_stats`), but needs a different data shape (PTY handoff-event correlation) than the existing reads-log buckets. Deferred as its own future task, explicitly not folded into T-082/T-083.
- Filed **T-082** (fix compression data source, read from `proxy_savings.json`, apply existing `_filter_by_window` scoping, never fabricate) and **T-083** (split report output into "Waste Avoided (shadow)" vs. "Real Savings Realized" sections; depends on T-082). Both written to `tasks.json` with full descriptions/owns, and added to `execution_plan.md` Milestone 4 addendum as Round J / Round K.

## Key decisions

- Classified as an **oracle** session for handoff purposes despite neither literal detection string matching (no `| UNRESOLVED |` row — the new section resolved everything inline; no `Status: IN_PROGRESS` in `execution_plan.md`) — matches the precedent set in the prior handoff: git/file evidence overrides the literal grep when they diverge. This session produced design decisions and task definitions, zero code changes — unambiguously oracle-shaped.
- Encoded a general report-semantics rule while at it: `get_bucketed_stats` buckets (idx, no-reread, indexing-gap) are shadow *waste* metrics, never realized savings — only `file_reads_saved`, `verbosity_savings`, `compression_savings` roll into `total_saved`. T-083 is the enforcement point for this in the presentation layer.
- Did not touch `report_builder.py` or `dashboard_template.html` this session — diagnosis only, implementation deferred to `/orchestrate` per the standard oracle → orchestrator handoff boundary.

## Open items / next steps

- **T-082 and T-083 are both PENDING, unstarted** — next `/orchestrate` session should pick these up (Milestone 4 addendum, Round J then Round K; T-083 depends on T-082).
- Milestone 5 (T-063 cross-provider claiming, T-064) remains independently PENDING from the prior session — not blocked by T-082/T-083, could be pulled forward instead if preferred.
- Session-recycling/handoff report bucket remains an unscoped future task — raise with oracle again when handoff-driven savings need to be demoed.
- `design_status.md`, `execution_plan.md`, `tasks.json`, `combined_report.html` are modified but uncommitted as of this handoff — this commit will include them.

## State document updated
`design_status.md` (new "Oracle Direction — Sparred 2026-07-02" section, all RESOLVED/DEFERRED); `execution_plan.md` (T-082/T-083 rows + Round J/K added to Milestone 4 addendum); `tasks.json` (T-082/T-083 full task definitions appended, status `pending`).
