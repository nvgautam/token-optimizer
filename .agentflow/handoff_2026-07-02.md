---
name: handoff-2026-07-02
description: Oracle re-spar — root-caused report savings swing (73%→23%), quantified verbosity A/B + idx attribution, filed T-084
session_type: oracle
---

## Completed this session
- Root-caused the 73%→23% `combined_report.html` swing: part expected (T-082/T-083's measurement fix removed inflated/fabricated numbers), part real regression
- Traced the regression to `is_cache_mode()` in headroom's `anthropic.py:911-915` — freezes every message but the latest turn for every tool, not just Read/Grep/Write/Edit as T-080's commit message assumed
- Quantified from `.headroom/logs/proxy.log`: pre-cutover 649/882 requests (74%) compressed, 8.96M tokens saved; post-cutover 8/920 requests (<1%), steady-state 0 (`transforms=none` on every request since)
- Answered: verbosity A/B savings are unmeasured (`sample_size: 0`, T-081's harness never run — still using the 600-token design guess, 6.1% of total_saved)
- Answered: idx-driven savings ARE in the 23% headline, via `file_reads_saved` (1,026,592 tokens, 9.6% of total_saved) — distinct from the "idx" bucket (correctly 0, a waste-only counter)
- Filed T-084 (pending) to find a narrower headroom config or consciously accept the compression trade-off

## Key decisions
- T-084 classified as independent (Round L) — no deps on other pending tasks
- Did not re-open the "session-recycling strategy missing from report" deferral (row 108) — user redirected to the swing investigation before that was finalized; still DEFERRED, unchanged

## Open items / next steps
- T-084 is next up for `/orchestrate`: investigate headroom config options to decouple Read/Grep protection from the all-but-latest-turn freeze; update `.agentflow/headroom_eval.md` + architecture.md's Headroom section with the measured trade-off
- Verbosity A/B (T-081 mechanism) still needs an operator to actually run paired hook-on/hook-off sessions — no code work, just execution
- Session-recycling report-gap deferral (design_status.md row 108) still open if user wants to revisit
- Prior handoff note (same day, orchestrator session): local `main` was 9 commits ahead of `origin/main`, unpushed — still true, not addressed this session

## State document updated
design_status.md, execution_plan.md, tasks.json
