---
name: handoff-2026-06-29g
description: Banner injection refactor + T-054/T-055 tasked; root cause analysis of verbosity compliance
session_type: orchestrator
---

## Completed this session
- Diagnosed root cause of banner-in-input-field bug: `write_input` to PTY stdin echoes characters into Claude Code TUI input buffer regardless of timing
- Refactored banner injection: replaced all `self._pty.write_input(banner)` calls with `self._pending_banner` queue in `session_manager.py`
- Added `pop_pending_banner()` method to `SessionManager`
- Updated `cli.py`: banner now injected at Enter keypress (appended to user message), never pre-populates input field
- Updated all affected tests in `tests/shell/test_session_manager.py` (banner assertions moved from `pty.inputs` to `sm._pending_banner`); 34/34 passing
- Added T-054 and T-055 to `tasks.json` as PENDING
- Added Round G to execution_plan.md (T-051, T-054, T-055)

## Key decisions
- Static banner (startup IDX + verbosity): CLAUDE.md already covers these; PTY injection is redundant for static content — only per-turn dynamic feedback needs the pending_banner mechanism
- `_pending_banner` approach (buffer + inject at Enter) fixes input-field echo; remaining issue is brief echo flash at submission moment (acceptable)
- `read_check.py` is currently advisory (exit 0 + print hint); enforcement requires exit 1 to block the tool call — T-054
- Verbosity banner must be turn-aware: generation turns (keywords: generate/write/create/proceed/implement/yes, or >80 chars) must not be suppressed — T-055
- Previous timing-based fixes (byte threshold, quiet period) failed because `write_input` always echoes; the fix requires changing the delivery channel, not the timing

## Open items / next steps
- T-051: CLI integration tests (PENDING — pre-existing)
- T-054: `read_check.py` exit 1 enforcement (PENDING — high impact, do first)
- T-055: `_pending_banner` generation-turn guard (PENDING)
- Untracked: consider whether static startup banners (verbosity + IDX) should be removed from PTY entirely since CLAUDE.md already covers them — would simplify session_manager.py

## State document updated
execution_plan.md — Round G added with T-051, T-054, T-055
