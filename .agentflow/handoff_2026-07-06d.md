---
name: handoff-2026-07-06d
description: PTY handoff bug fixes — wrong-time trigger root cause, Gemini token reduction, handoff_disabled flag
session_type: unknown
---

## Completed this session

- **T-122–T-126 status audit** — T-124+T-125 confirmed merged (e64e7ae97); T-122/T-126 genuinely pending; T-123 complete stub
- **Gemini auto-safety wrong-time trigger fixed** — root cause: session restart replays ~120K context; TASK_START not re-emitted → `_task_start_tokens` always empty after restart; fix: `task_in_flight` now checks `self._state_machine.state == States.TASK_RUNNING` (file-backed, survives restarts)
- **`handoff_disabled` flag file** — `.agentflow/handoff_disabled` toggles auto-handoff without PTY restart; gated in both `_handle_output` and `poll()`
- **Gemini token reduction** — provider-specific thresholds `[shell.gemini]` in `~/.agentflow/config.toml` (40K/60K/80K); session_manager reads override at init; expected ~57% inp/turn reduction
- **Hung Gemini processes killed** — PIDs 41720, 31478, 31475

## Key decisions

- `task_in_flight` now uses state machine state (file-backed, not in-memory dict) — survives restart context replay
- Gemini thresholds 40K/60K/80K vs Claude 80K/120K/150K — no prompt cache, cost is linear not amortized
- Flag file for handoff_disabled (not config) — per-session toggle without restart

## Open items / next steps

- `rm .agentflow/handoff_disabled` when testing session ends
- Commit session_manager.py changes before next Gemini session
- D3-prep: T-139 ‖ T-140 ‖ T-142 → T-141 (size splits, unblocks T-121/T-122)
- T-122 regression tests and T-126 idle wake-up still pending

## State document updated

design_status.md — "Oracle Direction — Sparred 2026-07-06d"
