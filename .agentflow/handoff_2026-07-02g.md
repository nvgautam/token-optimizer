---
name: handoff-2026-07-02g
description: Oracle re-spar — retroactivity of T-082/T-084/T-085/T-086 fixes resolved, steady-state pct_saved decision, T-087 filed
session_type: oracle
---

## Completed this session
- Diagnosed "session-cycling savings missing from combined_report.html": T-085's "Modeled Projections" section already exists in the uncommitted working copy — last *commit* just predates report_builder.py's T-085 change. No bug.
- Confirmed handoff/session-cycling figure is deliberately excluded from `total_saved`/`pct_saved` (modeled projection, not measured) — explained p25-vs-p50/p75 calibration choice (undershoot bias, bill-reconciliation credibility).
- Resolved retroactivity question: T-082/T-083/T-085 are counting-bug fixes → apply across full ledger history automatically (report recomputes from raw events each run). T-084/T-086 fixed *real* regressions (compression genuinely at ~0 during those windows) → must NOT be retroactively credited.
- Decided two-percentage report strategy: keep lifetime `pct_saved` (full history, most auditable) + add a new steady-state `pct_saved` scoped to post-T-084/T-086 sessions, for demo/investor use.
- Filed **T-087** (pending) in `execution_plan.md` + `tasks.json`: steady-state pct_saved via new `agentflow/reporting/steady_state.py`, keeping `report_builder.py` under the 250-line cap.
- Explained T-081's verbosity A/B mechanism (`agentflow/shadow/verbosity_ab.py`) — built but never exercised (n=0), no CLI wiring; requires a manual hook-off control period.

## Key decisions
- Demo/investor guidance: lead with measured `total_saved`/steady-state `pct_saved` once T-087 lands; treat the handoff modeled-projection number as forward-looking upside, never the headline.
- Do not truncate report history to "start from today" — full lifetime history stays, since it makes the regression-and-recovery trend visible (stronger investor story than a clean line).
- T-087 depends on T-084 + T-086 merge timestamps (both already merged) to define the steady-state window start — unblocked, ready for `/orchestrate`.

## Open items / next steps
- **T-087** pending — run `/orchestrate` to implement.
- `combined_report.html` has an uncommitted regenerated diff (T-085's Modeled Projections section) — user has not yet confirmed whether to commit it.
- Verbosity A/B (T-081 mechanism) unexercised — no task filed; purely optional, user-initiated when ready to accept a temporary hook-off control period.
- Milestone 5 (T-063 `STOPPED_BY_USER`, T-064) and Milestone 6 (T-068, T-069) still pending per `.agentflow/state.json`.

## State document updated
`execution_plan.md` + `tasks.json` (T-087 added directly during sparring, not via this flush). `design_status.md` unchanged this session.
