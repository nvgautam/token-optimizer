---
name: handoff-2026-06-29f
description: Bug fixes — verbosity banner quiet-period + read_check.py PreToolUse hook
session_type: unknown
---

## Completed this session
- Fixed verbosity static banner appearing in input field: replaced byte-count threshold (`_startup_bytes_seen >= 1024`) with quiet-period detection (`on_idle_tick`, default 1.5s); banner now fires only after Claude is idle
- Wired `on_idle_tick()` into `cmd_shell` loop (called on each 50ms timeout when no PTY output)
- Startup banner now also injects IDX protocol reminder (alongside verbosity), so Claude has it before any reads
- Created `agentflow/hooks/read_check.py` — PreToolUse hook that injects section map hint on full reads when `.idx` exists; 8 tests pass
- Added `read_check.py` to `.claude/settings.json` PreToolUse hooks (settings.json edit blocked by auto-mode; user approval needed)
- Reverted `read_logger.py` — stays as silent logger only

## Key decisions
- Quiet-period approach over byte-count: byte-count fires mid-generation (TUI busy); 1.5s silence = Claude truly idle, readline ready
- `on_idle_tick()` driven from cli.py loop (single-threaded) rather than background thread — testable with `startup_quiet_period_seconds=0.0`
- `read_check.py` separate from `read_logger.py` — logger stays silent, checker handles injection; matches pre-existing test spec in `tests/hooks/test_read_check.py`
- Hook output is "opaque" — no mention of "index"/"idx"/"symbol"/"token" per test_hint_output_is_opaque

## Open items / next steps
- User must approve `.claude/settings.json` change to wire up `read_check.py` (blocked by auto-mode classifier)
- T-051 still PENDING: CLI integration tests (argparse round-trip, main() dispatch, __main__ smoke)
- Pre-existing `test_cli_shell.py` failures (11 failures, `args.shell_command` AttributeError) — unrelated to this session

## State document updated
execution_plan.md — no change (no milestone tasks completed; bug fixes to merged tasks)
