---
name: handoff-2026-07-02k
description: Oracle re-spar — root-caused "Percentage Saved" 8.2% vs headroom's 65% claim; filed T-089/T-090
session_type: oracle
---

## Completed this session
- Reproduced `build_report()` live (14.5% now vs 8.2% baked into stale `combined_report.html`) — confirmed arithmetic is internally consistent, not silently broken.
- Root-caused the low "Percentage Saved": `compression_real` (headroom's cumulative `total_input_tokens` delta, ~95% of `total_real` in-window) is a cumulative resend cost; `file_reads_real`/`verbosity_real` are one-shot per-event costs. Blending all three swamps the blended % down to ≈compression's own local rate.
- Confirmed the low compression rate itself is expected (T-084's cache-mode fix), not a new bug.
- Root-caused the 8.2% vs 42.4% divergence: "Percentage Saved" and "Current/Steady-State Savings" measure two unrelated strategies (file-read+verbosity+compression vs handoff/session-recycling via `shadow_tracker.update_shadow`), not two versions of one number.
- Found a third, independent handoff estimate (`handoff_savings.py`, 44.7% pct_reduction) corroborating the 42.4% steady-state figure — but windowed differently (lifetime vs post-fix) and buried as a strategy row, not a headline stat.
- Confirmed session-recycling savings mechanically grow with session count (`shadow_extra = n_turns * accumulated_context`) — already the largest lever, just unlabeled.
- Also flagged (not yet task-filed): `dashboard_template.html:134`'s "(lifetime, all sessions)" label is stale since T-087's windowing commit — folded into T-089's scope.
- Filed T-089 (relabel stats + reconcile windowing across all savings sources, independent) and T-090 (unify all four strategies into one reconciled percentage, depends on T-089) in `tasks.json` + `execution_plan.md` (Rounds Q/R).
- Recorded root-cause findings as 3 new RESOLVED rows in `design_status.md`.

## Key decisions
- Did not attempt to fix the compression double-count or relabel stats in-session — scoped as implementation work for `/orchestrate`, not oracle sparring.
- Kept `handoff_savings.py`'s modeled/projected number out of `total_saved` (unchanged design stance, row "Report metric semantics") — its corroboration of steady_state's 42.4% is evidence the *number* is plausible, not license to blend an unmeasured projection into a measured total.

## Open items / next steps
- T-089 (pending, independent): relabel `dashboard_template.html` stats for actual scope; reconcile windowing — log-driven path uses `WINDOW_START`, `steady_state.py` uses ledger sessions ≥ `WINDOW_START` (N=5, still thin), `handoff_savings.py` ignores the window entirely (108 lifetime sessions).
- T-090 (pending, depends on T-089): combine all four strategies into one true overall percentage, after normalizing compression's cumulative-cost basis against the other three's one-shot basis.
- Milestone 5 (Multi-Provider Coordination) still has T-063/T-064/T-068/T-069 pending per `.agentflow/state.json` — unrelated to this session's work, unchanged.
- Let post-fix sample size grow before treating 42.4% steady-state as stable (N=5 only, ~7hrs of data at investigation time).

## State document updated
design_status.md (3 new RESOLVED rows), execution_plan.md (T-089/T-090 + Rounds Q/R), tasks.json (T-089/T-090 full defs)
