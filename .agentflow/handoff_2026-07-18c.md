---
name: handoff-2026-07-18c
description: Oracle session — C-2b drain miss root cause confirmed; T-278/T-279/T-280 filed; forensic investigation complete
session_type: oracle
---

## Completed this session

- **C-2b drain miss root cause confirmed** (forensic, log-only, no guessing):
  - C-2a used **Bash** (not Write tool) to write `current_round.json` → `sync_tif_fallback_written` at 06:45:28, not `sync_tif_written`. TIF was populated via fallback path (readable from disk).
  - C-2a drain fired correctly. `_write_merged_and_clear` ran and **unlinked both `current_round.json` AND `tasks_in_flight.json`**.
  - C-2b (T-268) started but wrote **neither** `current_round.json` nor populated TIF (no `sync_tif_written`, no `sync_tif_fallback_written` for T-268). TIF file was absent entirely.
  - Every subsequent `check_drain_restart` poll exited at condition 5 (`no_tasks_in_flight_file`): `tif.exists()` → False → `_skip("no_tasks_in_flight_file")`. Drain was structurally impossible for the entire C-2b round.
  - Secondary suppressor: context_fill.json ts=06:59:22 (fill=94,865); any poll >60s later would also fail `fill_stale` (condition 9).
  - The `context_fill_reset` at 06:52:28 (when T-162's task_complete was processed) reset fill to ~0; fill rebuilt to 94K over T-268's ~6.5-minute execution.
  - `drain_check_skip` events stopped after 06:47:28 because after C-2a drain cleaned up TIF, the new skip reason (`no_tasks_in_flight_file`) would have been throttled; and after T-268 the session went idle (no output → no drain polls).

- **T-278 filed (Round C-state, solo)**: Orchestrate resume — derive next round from execution_plan.md Master Round Table, not state.json.
- **T-279 filed (Round C-state2, parallel)**: Enforce Write tool (not Bash) for `current_round.json` in both orchestrate skills. Fixes the TIF bypass that caused C-2b miss.
- **T-280 filed (Round C-state2, parallel with T-279)**: Startup reconciliation — on orchestrate start, validate `current_round.json` task_ids against tasks.json; unlink if any are complete. Depends on T-278, T-279.
- **execution_plan.md updated**: Round C-state2 (T-279 ‖ T-280) inserted after Round C-state row.
- **tasks.json updated**: T-277 complete; T-278, T-279, T-280 added as pending.

## Key technical facts established (do not re-derive)

- **`_log()` in `post_tool_use.py` writes to `hook_drain_debug.jsonl`**, NOT `pty_audit.jsonl`. Drain-related hook events are in `hook_drain_debug.jsonl`.
- **`sync_tif_written`** = Write tool used for `current_round.json` with non-empty task_ids → TIF populated.
- **`sync_tif_fallback_written`** = Bash used → TIF populated from disk via fallback (correct but fragile).
- **`sync_tif_skip reason='not_write_tool'`** = Bash used, TIF NOT populated.
- **Orchestrator session = 93d396b8-e4cf-423e-a05c-839c19596aee** (confirmed from pty_audit drain_check_skip events; `session_state.json` shows "oracle" but that was written by the oracle SKILL, NOT the PTY's in-memory session_type).
- **`check_drain_restart` conditions (drain_restart.py:79–128)**:
  1. session_type == "orchestrator"
  2. 30s cooldown since last restart
  3. State in {IDLE, TASK_RUNNING}
  4. Not handoff_in_progress / auto_handoff_disabled
  5. TIF file exists → **the blocking condition for C-2b**
  6. TIF parseable
  7. round_exists OR tif_is_tombstone
  8. TIF is tombstone (tif_content falsy)
  9. context_fill.json ts < 60s old (fill_stale suppressor)
  10. fill_tokens ≥ 80K threshold

## Open discussion thread (carry forward)

The forensic investigation ended with a natural next question: **T-260 (CLI-as-interface for round announcements)** makes conditions 5–8 irrelevant — a single `agentflow round start --task-ids ...` CLI call atomically writes both `current_round.json` + TIF, removing the hook/tool_name dependency entirely. T-259 (spike) precedes it. These are already in the round table.

The open design question not yet resolved: **should T-260 fully supersede T-279**, or should both land (Write tool constraint as defense-in-depth while CLI is being built)? User had not yet weighed in on this when the session ended.

## Round table state

```
Round C-P4  | T-277       | MERGED (completed this session chain)
Round C-state | T-278     | pending (solo, next to run)
Round C-state2 | T-279 ‖ T-280 | pending (parallel, after T-278)
Round C-1   | T-266       | pending (debug.md rewrite)
Round C-2   | T-162 ‖ T-268 | COMPLETE (worktrees exist at .claude/worktrees/T-162-oracle-split and T-268-oracle-dup-check; both merged)
```

`state.json` says `next_round: "Round C-1"` — this is **stale**. Round C-state and C-state2 must run first. T-278 fixes stale next_round reads going forward.

## Files to read on next session start

- `execution_plan.md` — Master Round Table section (use .idx if present)
- `tasks.json` — verify T-278/T-279/T-280 status
- `.agentflow/hook_drain_debug.jsonl` — if more forensic context needed
- `agentflow/shell/drain_restart.py` — if drain logic needs further analysis
- `agentflow/hooks/post_tool_use.py` — if TIF sync logic needs further analysis
