---
name: handoff-2026-06-29c
description: Fix PTY banner \r→\n bug — banners now submitted to Claude
session_type: unknown
---

## Completed this session
- Diagnosed why `[VERBOSITY]` and `[IDX]` banners appeared in Claude Code input field but were never submitted
- Fixed all 4 banner terminators in `agentflow/shell/session_manager.py`: `\r` → `\n`
  - `_VERBOSITY_STATIC_BANNER` (startup injection)
  - `_IDX_BANNER` (every-3-turns injection)
  - Targeted idx injection in `_handle_output`
  - Dynamic verbosity banner in `_inject_verbosity_banner`

## Key decisions
- Root cause: Claude Code TUI runs in raw PTY mode — `\r` is carriage return only, not Enter; `\n` triggers submission
- All 4 banners were silently no-oping since T-052 merged; idx compliance and verbosity recalibration never fired
- Confirmed `.idx` files exist for `session_manager.py` and `pty_wrapper.py` — idx protocol was being skipped due to missing banner reinforcement

## Open items / next steps
- Commit the `\r` → `\n` fix in `session_manager.py`
- Verify banners are actually submitted in next PTY session (should see them as user messages in Claude Code)
- Consider whether submitted banners cause unwanted Claude responses (if so, may need a different injection strategy)

## State document updated
none
