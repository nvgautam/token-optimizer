---
name: handoff-2026-07-02i
description: Oracle re-spar — diagnosed combined_report.html metric semantics (no-reread/indexing-gap/state-docs), confirmed T-087 already covers the pct_saved windowing concern, filed T-088 for index-savings visibility
session_type: oracle
---

## Completed this session
- Explained `combined_report.html` strategy-row semantics: no-reread/indexing-gap are *waste occurred* (not savings), state-docs is raw read volume (mislabeled under "Real Savings"), verbosity is an unmeasured placeholder pending T-081's A/B run, compression is the only currently-real measured savings row.
- Traced `pct_saved` windowing bug: `reporting_window` (report_builder.py:124) spans shadow_reads.jsonl's full lifetime while compression is effectively windowed to ~1 day since T-084/T-086 landed — confirmed this is exactly what T-087 (filed same day, PENDING) already addresses. No duplicate task filed.
- Filed **T-088**: surface index-driven read savings (`file_reads_saved`, currently only rolled into the aggregate `total_saved` headline) as its own report row, partitioned from generic offset/limit savings. Added to `tasks.json`, `execution_plan.md` (table row + Round P, independent), `design_status.md`.
- Gave concrete manual steps to actually run the T-081 verbosity A/B baseline (`AGENTFLOW_VERBOSITY_HOOK_DISABLED=1` sessions, `import_from_verbosity_log`, `run_verbosity_ab`) — confirmed no CLI entrypoint wires this yet; offered to file a wrapper task, user hasn't confirmed.

## Key decisions
- `design_status.md`: two new rows — "Index-driven savings not surfaced in report" (RESOLVED → T-088) and "pct_saved blends pre/post-regression windows" (SUPERSEDED — already covered by T-087, no new task).
- Did not implement T-087 or T-088 in this session — oracle files/specs tasks, `/orchestrate` implements. Both are unblocked (T-087 deps T-084/T-086 already MERGED; T-088 independent).

## Open items / next steps
- T-087 (steady-state pct_saved) and T-088 (index-savings line item) both PENDING, ready for `/orchestrate`.
- Verbosity A/B baseline still unrun (n=0) — user has not yet decided whether to run it manually or wants a CLI wrapper task filed.
- Milestone 5 (Multi-Provider Coordination) still next after the M4 addendum tasks clear; T-063/T-064/T-068/T-069 remain pending per `.agentflow/state.json`.

## State document updated
design_status.md, execution_plan.md, tasks.json
