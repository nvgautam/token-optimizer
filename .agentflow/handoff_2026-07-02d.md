---
name: handoff-2026-07-02d
description: T-084 merged (PR #48) — headroom compression regression fixed, residual gate-file risk trade-off documented
session_type: orchestrator
---

## Completed this session
- T-084 implemented, reviewed, and merged (PR #48): reverted `agentflow/cli.py`'s `cmd_shell` from `HEADROOM_MODE=cache` back to `HEADROOM_MODE=token`.
- Root cause confirmed by reading the installed `headroom-ai` 0.28.0 source directly: `DEFAULT_EXCLUDE_TOOLS` (config.py) already protects Read/Glob/Grep/Write/Edit output unconditionally in *both* modes — cache mode's `is_cache_mode()` unconditional `_strict_previous_turn_frozen_count()` (anthropic.py:908-915) never added tool-type protection that wasn't already there. It just froze every message except the current turn, killing ContentRouter compression from 74% of requests to <1% (measured via `.headroom/logs/proxy.log`).
- Confirmed via exhaustive grep of `server.py`/`content_router.py`/`anthropic.py` that no upstream knob decouples `protect_recent_reads_fraction` from the token/cache mode dichotomy — filing an upstream feature request was left out of scope.
- Documented the regression and fix in `.agentflow/headroom_eval.md` (new section 10) and added a short pointer note to `architecture.md`'s ContentRouter section (lines 446-471 only).
- Added `TestHeadroomWrap` (3 tests) to `tests/test_cli_shell.py`, which previously had zero headroom-specific coverage.
- Pass 1 (programmatic) and Pass 2 (fresh Haiku LLM review, 3 checklists) both CLEAN.

## Key decisions
- **Residual corruption risk accepted, not eliminated.** Reverting to token mode reintroduces the theoretical leak vector token mode always had: `protect_recent_reads_fraction=0.3` means excluded-tool output older than ~30% of the conversation can still fall through to compression. This is NOT re-guarded at the proxy level by this fix.
- **User explicitly raised this** ("won't the original problem of corrupted content resurface?") before approving the merge. Answer given and confirmed: T-79's actual fix was never proxy-level — it's a skill-level standing rule already in `oracle.md`/`orchestrate.md` requiring a fresh same-turn Read before acting on any gate file's contents, with immediate re-read on anything garbled-looking. That rule is independent of headroom mode and was untouched by this PR, so it continues to bound the residual risk. User approved the merge with this trade-off understood, not because the risk is zero.
- Scope of T-084 was narrowed at spawn time from the oracle-filed task's owns list (`agentflow/cli.py`, `.agentflow/headroom_eval.md`) to also include `architecture.md` (matching the task description's step 3, which named it but the owns list omitted) and `tests/test_cli_shell.py` (added for TDD coverage — the file had zero coverage of the headroom-wrap branch despite T-074 claiming otherwise, a pre-existing drift not fixed here beyond this task's slice).

## Open items / next steps
- T-085 (pending): calibrate handoff/session-recycling savings from real ledger data (p25 tok/turn from 99 sessions), wire into `report_builder.py` as a new line item.
- T-063 (pending, previously `STOPPED_BY_USER`): cross-provider atomic task claiming — needs explicit user go-ahead before respawning.
- T-064 (pending, blocked on T-063), T-068 (pending, ready — per-task token estimator), T-069 (pending, blocked on T-068).
- No upstream feature request filed against headroom-ai for a knob that decouples `protect_recent_reads_fraction` from the mode dichotomy — worth revisiting if the 0.3-fraction gate-file leak proves material in practice (no incident yet under token mode's restored config; T-79's original incident predates this fix and was itself skill-mitigated, not proxy-mitigated).
- Local `main` was 12 commits ahead of `origin/main` before this session's merge reconciliation (clean, no conflicts) — not pushed to `origin/main` beyond what `gh pr merge` landed automatically. Uncommitted living-doc changes remain in the working tree pending this handoff's own flush commit.

## State document updated
`tasks.json` (T-084 → complete), `.agentflow/tasks.archive.json` (T-084 full definition appended), `execution_plan.md` (T-084 row + addendum round L → MERGED), `.agentflow/state.json` (round L entry added, pending list updated to include T-085, T-084 removed)
