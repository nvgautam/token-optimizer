---
name: handoff-2026-07-06f
description: Diagnosed PTY ANSI leak + stdin gating bug causing oracle session abort; filed T-143 + T-144
session_type: unknown
---

## Completed this session
- Diagnosed root cause of oracle session abort (session c27de4ec, 13:37:44–47)
- Identified two bugs: (1) ANSI escape code parameters visible on screen during PTY startup due to no SGR reset on spawn; (2) user stdin forwarded immediately to PTY child — Enter pressed to dismiss visual artifact sent to oracle, aborting the startup sequence
- Filed **T-143** (emit `\x1b[0m` on PTY start/restart in `cli.py` + `process_manager.py`) and **T-144** (suppress stdin forwarding 2–3 s after spawn in `cli.py` + `session_manager.py`, depends on T-143)
- Added both to `tasks.json`, `execution_plan.md` addendum, `state.json` pending list

## Key decisions
- T-143 before T-144: SGR reset must ship first; it's a 1-line fix and reduces the trigger condition for T-144
- stdin gating window: 2–3 s (not event-driven) to keep it stdlib-only and deterministic; event-driven would require detecting Ink's first full render, which is fragile

## Open items / next steps
- T-143 + T-144 are ready for `/orchestrate`
- Remaining D3 size splits: T-139 (`orchestrate.md`), T-141 (`test_session_manager.py`), T-142 (`verbosity_ab.py`)
- T-122 (session restart regression tests) still pending

## State document updated
- `tasks.json` — T-143 + T-144 added as pending
- `execution_plan.md` — addendum for T-143 + T-144 added
- `.agentflow/state.json` — T-143 + T-144 added to pending list
