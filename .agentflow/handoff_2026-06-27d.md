---
name: handoff-2026-06-27d
description: T-038 merged (universal .idx reading protocol in CLAUDE.md); T-039 added (ledger-anchored cap derivation); M3 complete
session_type: orchestrator
---

## Completed this session
- Fixed off-by-one in orchestrate.md step 6: `offset=start-1` → `offset=start`
- Merged T-038: added `## Reading protocol` section to CLAUDE.md — universal .idx rule for all sessions
- Added T-039: ledger-anchored rate cap derivation for orchestrate skill (M4 Round A)
- Added `Rate cap derivation` row to design_status.md (RESOLVED)
- Pruned T-038 worktree

## Key decisions
- Universal .idx rule belongs in CLAUDE.md (one place), not repeated per skill — replaces earlier per-skill T-038/T-039 plan
- Ledger timestamps are local time (`datetime.now()`, no tz arg in agentflow.py:463) — confirmed from source
- Cap derivation: `cap = total_billable_since_window_start / (current_pct / 100)` — more accurate than single-session delta; billable = uncached_input + cache_creation + output
- Current-session tokens (not yet flushed to ledger) are a known gap — correction: add current-session delta explicitly at calibration write time
- T-039 placed in M4 Round A (parallel with T-002, T-006, T-007) — unblocked, touches orchestrate.md only

## Open items / next steps
- M4 Round A: T-002, T-006, T-007, T-039 (all unblocked, run in parallel)
- M4 Round B: T-008 (depends on Round A)
- Uncommitted: design_status.md, execution_plan.md, tasks.json, .agentflow/state.json — commit before next spawn

## State document updated
execution_plan.md, design_status.md
