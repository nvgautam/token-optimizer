---
name: handoff-2026-06-29
description: T-052 merged (PTY Read-event idx injection + verbosity banners); AGENTFLOW_PTY env var added; T-051 pending
session_type: orchestrator
---

## Completed this session
- T-052 MERGED (PR #35) — `session_manager.py`: `_ansi_strip`, `_detect_read_path`, Read-event idx injection, `tick()` verbosity timer, session-start verbosity banner
- Committed prior-session fixes: `__main__.py` shim, argparse `dest="shell_command"` fix, `pty_wrapper._sync_winsize()` refactor
- `AGENTFLOW_PTY=1` env var added to `pty_wrapper.py` child process — queryable signal for PTY detection
- T-052 rework: tightened `_READ_PATH_RE` regex (false positive fix), expanded ANSI char class, absolute path guard, double-injection dedup

## Key decisions
- `tick()` called from `_handle_output` (fires on every output chunk) — avoids touching `cli.py` (not in T-052 owns)
- `AGENTFLOW_PTY=1` set via `os.environ` before `os.execvp` — inherited by child; 1-line change, no new API
- Worktree squash-merge left cosmetic `_IDX_BANNER` wording diff; confirmed all functional T-052 additions present in main; worktree pruned

## Open items / next steps
- T-051 PENDING: CLI integration tests — argparse round-trip, `main()` dispatch, `__main__` smoke (owns `tests/test_cli_shell.py`)
- After T-051: M4 COMPLETE → decompose M5 lazily
- PTY `HANDOFF RECOMMENDED` detection: currently not wired — PTY only watches token count; structured PTY signals deferred to v2
- T-051 scope note: `tests/test_cli_shell.py` existing `_args()` helper uses `ns.command`; the argparse fix changed dest to `shell_command` — tests need updating

## State document updated
execution_plan.md — T-052 → MERGED
tasks.json — T-052 status → complete
.agentflow/state.json — Round F MERGED, T-051 pending
