---
name: handoff-2026-07-05d
description: Oracle session — T-118 filed (ledger dedup); Gemini PTY handoff root cause analysis
session_type: oracle
---

## Completed this session
- Diagnosed 66.5% → 58.4% aggregate savings discrepancy: stale HTML + restart-loop duplicate ledger entries
- Identified two duplicate classes in agentflow_ledger.json: same session_id (multi-close) + same-fingerprint pairs (identical input_tokens/output_tokens/n_turns within 5-min window from restart loop)
- Filed T-118: ledger_dedup.py — two-rule dedup (max-n_turns per session_id; max-shadow_extra per fingerprint), one-time CLI migration + read-time guard in _handoff_component()
- Updated Round D: T-118 now first (before T-116 and IP stack)
- Diagnosed Gemini mid-task handoff: no AGENTFLOW_TASK_START signals emitted → in-flight guard blind; safety threshold (120K) fired at 128K; handoff aborted after 2-min timeout (HANDOFF_COMPLETE never received)
- Identified T-116 thread-safety race: _run_handoff_core background thread and main loop both call _pty.read_output() concurrently; double session_type_transition at 01:52:10 is the symptom

## Key decisions
- Ledger dedup uses max-shadow_extra (conservative) for same-fingerprint pairs — understates savings, more defensible for external audiences
- T-118 placed before T-116 in Round D: clean the data before compiling the IP stack
- 30s post-restart cooldown in session_manager.py (line 183) prevents death loop — must be committed before Round D begins
- trigger=auto source not fully explained: possibly running session predating 9d0def8 (01:33 commit), possibly T-116 concurrency race — needs targeted investigation before T-116 ships

## Open items / next steps
- T-118: implement ledger_dedup.py + read-time guard in _handoff_component() + tests (Round D first)
- Gemini task bracket reliability: verify AGENTFLOW_TASK_START/COMPLETE consistently emitted in practice before worker spawn
- T-116 thread-safety: _pty.read_output() called concurrently from background thread + main loop — needs lock or single-threaded redesign
- trigger=auto origin: audit all callpaths — no current code produces default param; unexplained
- Stale zombie agy PID 41720 (running since Fri 7PM) — kill on next session start

## State document updated
design_status.md, execution_plan.md, tasks.json
