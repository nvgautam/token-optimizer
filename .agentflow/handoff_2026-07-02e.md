---
name: handoff-2026-07-02e
description: Diagnosed two live-session headroom incidents (hook cwd corruption, silent wrap-skip); T-086 filed
session_type: oracle
---

## Completed this session
- Diagnosed stuck-session incident: `headroom wrap` corrupted the wrapped `claude` process's cwd (likely in its compiled Rust core, not its Python source); `.claude/settings.json` hooks used relative paths and broke. Fixed directly by anchoring all hook commands on `$CLAUDE_PROJECT_DIR` — applied inline (small, local, reversible config edit, no task/PR needed).
- Clarified T-084 (`HEADROOM_MODE` cache→token) restart semantics: env var + code only take effect at fresh PTY launch, not mid-session. Confirmed PR #48 merged.
- Root-caused "compression gains not going up" report: `lsof` on the live `claude` process showed direct ESTABLISHED connections to Anthropic edge IPs on :443 — no local headroom proxy in the request path at all. `.headroom/proxy_savings.json` history had zero new entries since before the session started, confirming zero (not reduced) compression.
- Traced this to `cmd_shell()`'s `AGENTFLOW_ENABLE_HEADROOM` gate depending on the ambient shell environment of whatever launched `python -m agentflow shell` — the one remaining non-cmd_shell-owned dependency (`HEADROOM_WORKSPACE_DIR`/`HEADROOM_MODE` are already set unconditionally by cmd_shell itself).
- Filed **T-086**: move the headroom-wrap toggle into `agentflow/config/` (default-on when `headroom` is on PATH), keep `AGENTFLOW_ENABLE_HEADROOM` as an explicit override only, add a startup active/inactive banner. Logged in `tasks.json`, `execution_plan.md` (Round N), `design_status.md`.

## Key decisions
- Hook-path fix applied directly rather than routed through orchestrate/PR — config-only, local, reversible, urgent (user was blocked). Distinguished from T-086, which is a real code change (config schema + cli.py + tests) and goes through the normal task/PR flow.
- Root cause for both incidents is the same failure class: something outside AgentFlow's control (ambient shell state, or headroom's own process behavior) silently disabling a feature with no visible signal. T-086's startup banner is meant to close this class of bug generally, not just this one instance.

## Open items / next steps
- T-086 is `pending` — run `/orchestrate` to implement (config schema + defaults.yaml + cli.py + startup banner + tests).
- Residual, not filed: no audit yet of whether anything *besides* hooks assumes cwd == project root while running under `headroom wrap` (flagged as a residual risk in `design_status.md`'s hook-path-resolution row, not scoped into a task).

## State document updated
- `design_status.md` — 2 new RESOLVED rows (hook path resolution incident; headroom silent-skip incident)
- `execution_plan.md` — T-086 added to Milestone 4 addendum table + Round N
- `tasks.json` — T-086 added, status `pending`
