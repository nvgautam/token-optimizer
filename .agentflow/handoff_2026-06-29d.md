---
name: handoff-2026-06-29d
description: Oracle session — two PTY session_manager bug fixes, test cleanup, T-052/T-053 task state
session_type: oracle
---

## Completed this session
- Fixed verbosity static banner firing at `__init__` time — deferred to first `_handle_output` chunk via `_initial_banner_sent` flag; banner no longer appears in input field on restart
- Fixed T-052 IDX reactive banner never firing — `not detected_path.startswith("/")` guard blocked all Read calls (Read always uses absolute paths); now strips cwd prefix, drops paths outside project
- Fixed 3 pre-existing test failures: verbosity threshold test missing 30s cooldown bypass; two `tick()` tests referencing removed method replaced with banner-timing tests
- Added 5 new tests: static banner not at init, fires on first output, fires once; absolute path idx injection; outside-cwd path silently ignored
- tasks.json: fixed T-052 duplicate `status` field (was reading as PENDING); added T-053 (complete) for both bug fixes

## Key decisions
- `_initial_banner_sent` flag chosen over sleep — deterministic, no timing dependency
- Absolute paths outside cwd silently dropped (no banner, no error) — system paths like `/etc/passwd` correctly ignored
- `tick()` tests removed — method was never implemented; per-turn verbosity already covered by `_inject_verbosity_banner` tests

## Open items / next steps
- T-051 still PENDING: CLI integration tests (argparse round-trip, main() dispatch, `__main__` smoke)
- M4 remains IN_PROGRESS until T-051 merged
- Verify in live PTY session that IDX reactive banner now fires correctly for Read calls (absolute path fix unverified in prod)

## State document updated
tasks.json — T-052 status corrected to complete; T-053 added as complete
